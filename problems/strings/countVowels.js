/* ------------------------------------------------
     Part 4 (Easy): countVowels(str)
     Count vowels a/e/i/o/u (ignore case).
     Edge: "" → 0
     Hint search: mdn string toLowerCase, mdn includes
  -------------------------------------------------- */
function countVowels(str) {
  // TODO: implement
}
// ✅ Tests (uncomment after you implement)
// console.log("Part 4");
// console.assert(countVowels("Hello") === 2, 'countVowels("Hello") → 2');
// console.assert(countVowels("xyz") === 0, 'countVowels("xyz") → 0');
// console.assert(countVowels("") === 0, 'countVowels("") → 0');
